import React from 'React';
import {useState} from 'React'  

const Card = (props) => {
    props.contentObj
}

return (
    <div>
        <Card classname = "forceGraphCard"
            
        />
    </div>
)